#ifndef TENSOR_ARRAY
#define TENSOR_ARRAY
#include VertexTensor
class TENSOR_ARRAY{
private:
    VertexTensor* array;
public:
    void RotateArray(float angle_x, float angle_y, float angle_z);
}

#endif
